server:
  port: 8080
  servlet:
    context-path: /

mybatis:
  mapper-locations: classpath:*xml
  type-aliases-package: cn.nero.ds.domain

spring:
  datasource:
    dynamic:
      primary: user
      strict: false
      datasource:
        user:
          url: jdbc:mysql://localhost:3306/user?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
          username: root
          password: 2018023931
        balance:
          url: jdbc:mysql://rm-2ze4w314o6rc8sxy8do.mysql.rds.aliyuncs.com/balance?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
          username: dev
          password: "!eGzZcsh8NhDFY+q"